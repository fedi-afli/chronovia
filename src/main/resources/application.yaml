spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chronovia_db
    username: postgres
    password: 99016887
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect